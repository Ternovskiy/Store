@model List<DataModul.DomainModel.Category>


@if (Model!=null)
//{foreach (var item in Model)
{for (int i = 0; i < Model.Count; i++)
{
    @Html.HiddenFor(p => Model[i].CategoryId)
    @Html.DisplayFor(p=>Model[i].Name)
    <button type="button" onclick="@{
    Model.Remove(Model[i]);
                     }">Del</button>  
}
}

@*@Html.DropDownListFor(model => model.Category, ViewBag.DropCategory as List<SelectListItem>, htmlAttributes: new { @class = "form-control" })*@
